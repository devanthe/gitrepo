#include <iostream>
#include <cmath>

using namespace std;
    float a, b, c, nmn, nw, mdm;
    set<getw> g = {}

int main(int argc, char **argv)
{
    g = {a, b, c};
    cout << "Podaj a:" << endl;
    cin >> a;
    cout << "Podaj b:" << endl;
    cin >> b;
    cout << "Podaj c:" << endl;
    cin >> c;
    nmn = min(set);
    set::erase(min(set));
    mdm = min(set);
    set::erase(min(set));
    nw = min(set);
    /*if(a >= b){
            if(a >= c){max = a;}else{max = c;}}else if(b >= c){max = b;}else{max = c;}
    if(a <= b){
            if(a <= c){min = a;}else{min = c;}}else if(b <= c){min = b;}else{min = c;}
    cout << min << endl << med << endl << max << endl;*/
    
    return 0;
}
